schema: proagents/registry-source/v1

id: npm

name: npm

type: package-registry

capabilities:
  search: true
  metadata: true
  resolve: false
  install: false

artifact_types:
  - tool

policy:
  allowed: true
