{
  "title": "Never merge a test that failed more than once in CI without a diagn…",
  "body": "Never merge a test that failed more than once in CI without a diagnosed root cause and a fix or an explicit quarantine ticket."
}
