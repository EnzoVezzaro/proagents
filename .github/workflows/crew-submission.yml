name: Marketplace proposal pipeline (crews & profiles)

# Marketplace submissions are GitHub issues, and this workflow is the gate:
#
#   1. VALIDATE — every crew-proposal or profile-proposal issue
#      (opened/edited/reopened) gets its CREW-JSON or PROFILE-JSON block
#      extracted and run through the same deterministic validators the CLI and
#      SPA use (src/crew/validate.ts, src/profiles/validation.ts). A comment
#      reports the verdict; editing the issue re-runs the check.
#
#   2. PUBLISH — a maintainer with write access comments `/publish` on a green
#      proposal; the workflow commits items/<id>.json + catalog.json to main
#      and the next Pages build serves it. `/close <reason>` rejects.
#
# Security notes:
#   - The extracted JSON is written to a temp file by the extractor itself —
#     never interpolated into shell (no injection surface).
#   - /publish requires write (or admin) permission on the repo.
#   - The JSON markers are defined in web/src/proposal.ts (single source of truth).

on:
  issues:
    types: [opened, edited, reopened]
  issue_comment:
    types: [created]

permissions:
  issues: write
  contents: write

concurrency:
  group: proposal-${{ github.event.issue.number }}
  cancel-in-progress: false

env:
  JSON_BEGIN: "<!-- CREW-JSON-BEGIN -->"
  JSON_END: "<!-- CREW-JSON-END -->"
  PROFILE_JSON_BEGIN: "<!-- PROFILE-JSON-BEGIN -->"
  PROFILE_JSON_END: "<!-- PROFILE-JSON-END -->"

jobs:
  validate:
    # Validation runs on the issue lifecycle; comment events skip to publish.
    if: github.event_name == 'issues'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci && npm run build

      - name: Extract proposal JSON (crew or profile)
        id: extract
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('node:fs');
            const { JSON_BEGIN, JSON_END, PROFILE_JSON_BEGIN, PROFILE_JSON_END } = process.env;
            const body = context.payload.issue?.body ?? '';
            // Kind detection: profile markers win if present, else crew markers.
            const pi = body.indexOf(PROFILE_JSON_BEGIN);
            const pj = body.indexOf(PROFILE_JSON_END);
            const isProfile = pi !== -1 && pj > pi;
            const BEGIN = isProfile ? PROFILE_JSON_BEGIN : JSON_BEGIN;
            const END = isProfile ? PROFILE_JSON_END : JSON_END;
            const i = body.indexOf(BEGIN);
            const j = body.indexOf(END);
            core.setOutput('itemKind', isProfile ? 'profile' : 'crew');
            if (i === -1 || j === -1 || j <= i) {
              fs.writeFileSync('/tmp/proposal-item.json', '');
              core.setOutput('hasJson', 'false');
              core.setOutput('crewId', '?');
              core.setOutput('crewVersion', '');
              return;
            }
            const fenced = body.slice(i + BEGIN.length, j).trim();
            const m = fenced.match(/```(?:json)?\s*([\s\S]*?)```/);
            const json = (m ? m[1] : fenced).trim();
            fs.writeFileSync('/tmp/proposal-item.json', json);
            core.setOutput('hasJson', 'true');
            let id = '?', version = '';
            try {
              const item = JSON.parse(json);
              id = String(item.id ?? item.profile?.slug ?? '?');
              version = String(item.version ?? item.profile?.version ?? '');
            } catch { /* keep defaults */ }
            core.setOutput('crewId', id);
            core.setOutput('crewVersion', version);

      - name: Validate
        id: validate
        if: steps.extract.outputs.hasJson == 'true'
        run: |
          if node --input-type=module -e "
            import { crewProblems } from './dist/crew/validate.js';
            import { profileProblems } from './dist/profiles/marketplace.js';
            import fs from 'node:fs';
            const item = JSON.parse(fs.readFileSync('/tmp/proposal-item.json', 'utf8'));
            const kind = '${{ steps.extract.outputs.itemKind }}';
            const problems = kind === 'profile' ? profileProblems(item) : crewProblems(item);
            for (const p of problems) console.error(p);
            process.exit(problems.length === 0 ? 0 : 1);
          " 2>/tmp/problems.txt; then
            echo "valid=true" >> "$GITHUB_OUTPUT"
          else
            echo "valid=false" >> "$GITHUB_OUTPUT"
            echo "problems<<PROBLEMS_EOF" >> "$GITHUB_OUTPUT"
            cat /tmp/problems.txt >> "$GITHUB_OUTPUT"
            echo "PROBLEMS_EOF" >> "$GITHUB_OUTPUT"
          fi

      - name: Comment verdict
        uses: actions/github-script@v7
        with:
          script: |
            const hasJson = '${{ steps.extract.outputs.hasJson }}' === 'true';
            const valid = '${{ steps.validate.outputs.valid }}' === 'true';
            const kind = '${{ steps.extract.outputs.itemKind }}';
            const id = '${{ steps.extract.outputs.crewId }}';
            const version = '${{ steps.extract.outputs.crewVersion }}';
            const problems = `${{ steps.validate.outputs.problems }}`.trim();
            const L = [];
            if (!hasJson) {
              L.push('❌ **No proposal JSON found.** The issue body must contain a `' + process.env.JSON_BEGIN + '` … `' + process.env.JSON_END + '` block (crew) or `' + process.env.PROFILE_JSON_BEGIN + '` … `' + process.env.PROFILE_JSON_END + '` block (profile) with a ```json fence.');
              L.push('', 'The [builder](https://enzovezzaro.github.io/proagents/) generates crew JSON automatically; profile proposals follow `profiles/security-engineer.json`.');
            } else if (valid) {
              L.push('✅ **`' + id + '` v' + version + ' passes the deterministic ' + kind + ' validator.**');
              L.push('', 'Next step: a maintainer reviews the design' + (kind === 'profile' ? ' (rules, verification, tools)' : ' (permissions, handoffs, MCP)') + ', then comments **/publish** on this issue to commit it to the marketplace catalog. Nothing goes live without that review.');
            } else {
              L.push('❌ **`' + id + '` failed validation**');
              L.push('', '```', problems || 'unknown error — check the workflow log', '```');
              L.push('', 'Edit the issue body to fix the JSON — the check re-runs automatically. The [builder](https://enzovezzaro.github.io/proagents/) only produces valid crews.');
            }
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: L.join('\n'),
            });

  publish:
    # /publish or /close (write access only) acts on a crew-proposal or
    # profile-proposal issue.
    if: >-
      github.event_name == 'issue_comment' &&
      github.event.action == 'created' &&
      !github.event.issue.pull_request &&
      (contains(github.event.issue.labels.*.name, 'crew-proposal') ||
       contains(github.event.issue.labels.*.name, 'profile-proposal'))
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci && npm run build

      - name: Check command & permission
        id: gate
        uses: actions/github-script@v7
        with:
          script: |
            const comment = (context.payload.comment?.body ?? '').trim();
            const command = comment.startsWith('/publish') ? 'publish'
              : comment.startsWith('/close') ? 'close'
              : '';
            core.setOutput('command', command);

            let allowed = 'false';
            if (command !== '') {
              const perm = await github.rest.repos.getCollaboratorPermissionLevel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                username: context.payload.comment.user.login,
              });
              allowed = ['admin', 'write'].includes(perm.data.permission) ? 'true' : 'false';
            }
            core.setOutput('allowed', allowed);
            core.setOutput('commandName', command);

      - name: "No permission → refuse politely"
        if: steps.gate.outputs.command != '' && steps.gate.outputs.allowed == 'false'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🚫 `/' + '${{ steps.gate.outputs.commandName }}' + '` requires write access to this repository.',
            });

      - name: Extract proposal JSON
        if: steps.gate.outputs.allowed == 'true' && steps.gate.outputs.command == 'publish'
        id: extract
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('node:fs');
            const { JSON_BEGIN, JSON_END, PROFILE_JSON_BEGIN, PROFILE_JSON_END } = process.env;
            const body = context.payload.issue?.body ?? '';
            const pi = body.indexOf(PROFILE_JSON_BEGIN);
            const pj = body.indexOf(PROFILE_JSON_END);
            const isProfile = pi !== -1 && pj > pi;
            const BEGIN = isProfile ? PROFILE_JSON_BEGIN : JSON_BEGIN;
            const END = isProfile ? PROFILE_JSON_END : JSON_END;
            core.setOutput('itemKind', isProfile ? 'profile' : 'crew');
            const i = body.indexOf(BEGIN);
            const j = body.indexOf(END);
            if (i === -1 || j === -1 || j <= i) {
              fs.writeFileSync('/tmp/proposal-item.json', '');
              core.setOutput('hasJson', 'false');
              return;
            }
            const fenced = body.slice(i + BEGIN.length, j).trim();
            const m = fenced.match(/```(?:json)?\s*([\s\S]*?)```/);
            fs.writeFileSync('/tmp/proposal-item.json', (m ? m[1] : fenced).trim());
            core.setOutput('hasJson', 'true');

      - name: Validate before publish
        id: validate
        if: steps.extract.outputs.hasJson == 'true'
        run: |
          if node --input-type=module -e "
            import { crewProblems } from './dist/crew/validate.js';
            import { profileProblems } from './dist/profiles/marketplace.js';
            import fs from 'node:fs';
            const item = JSON.parse(fs.readFileSync('/tmp/proposal-item.json', 'utf8'));
            const kind = '${{ steps.extract.outputs.itemKind }}';
            const problems = kind === 'profile' ? profileProblems(item) : crewProblems(item);
            for (const p of problems) console.error(p);
            process.exit(problems.length === 0 ? 0 : 1);
          " 2>/tmp/problems.txt; then
            echo "valid=true" >> "$GITHUB_OUTPUT"
          else
            echo "valid=false" >> "$GITHUB_OUTPUT"
            echo "problems<<PROBLEMS_EOF" >> "$GITHUB_OUTPUT"
            cat /tmp/problems.txt >> "$GITHUB_OUTPUT"
            echo "PROBLEMS_EOF" >> "$GITHUB_OUTPUT"
          fi

      - name: Refuse invalid /publish
        if: steps.extract.outputs.hasJson != 'true' || steps.validate.outputs.valid == 'false'
        uses: actions/github-script@v7
        with:
          script: |
            const problems = `${{ steps.validate.outputs.problems }}`.trim() || 'no CREW-JSON block found';
            const L = ['⚠️ /publish refused — the JSON does not pass validation:', '', '```', problems, '```', '', 'Fix the JSON first; the validate job re-runs on every edit.'];
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: L.join('\n'),
            });

      - name: Commit to catalog
        if: steps.validate.outputs.valid == 'true'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          ITEM_KIND: ${{ steps.extract.outputs.itemKind }}
        run: |
          node --input-type=module <<'NODE' > /tmp/publish-out.txt
            import { publishCrew } from "./dist/crew/registry.js";
            import { publishProfile } from "./dist/profiles/marketplace.js";
            import fs from "node:fs";
            const item = JSON.parse(fs.readFileSync("/tmp/proposal-item.json", "utf8"));
            const kind = process.env.ITEM_KIND;
            const publish = kind === "profile" ? publishProfile : publishCrew;
            const paths = await publish(item, {
              repo: process.env.GITHUB_REPOSITORY,
              branch: "main",
              token: process.env.GITHUB_TOKEN,
              authorName: "proagents-marketplace-bot",
              authorEmail: "noreply@github.com",
            });
            const id = kind === "profile" ? item.profile.slug + "@" + item.profile.version : item.id + "@" + item.version;
            console.log(id);
            console.log("committed " + paths.itemPath + " and " + paths.catalogPath);
          NODE
          echo "PUBLISHED=$(head -1 /tmp/publish-out.txt)" >> "$GITHUB_ENV"
          cat /tmp/publish-out.txt

      - name: Confirm & close
        if: steps.validate.outputs.valid == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const L = [
              '🚀 **Published `' + process.env.PUBLISHED + '` to the marketplace catalog** (via /publish by @' + context.payload.comment.user.login + ').',
              '',
              'It appears in the [marketplace](https://enzovezzaro.github.io/proagents/) after the next Pages build — usually under a minute. Closing as completed. 🎉',
            ];
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: L.join('\n'),
            });
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              state: 'closed',
              labels: [
                ...context.payload.issue.labels.map((l) => l.name).filter((n) => n.endsWith('-proposal')),
                'published',
              ],
            });

      - name: Close rejected proposal
        if: steps.gate.outputs.allowed == 'true' && steps.gate.outputs.command == 'close'
        uses: actions/github-script@v7
        with:
          script: |
            const reason = (context.payload.comment.body ?? '').replace(/^\s*\/close\b/, '').trim();
            const body = reason
              ? 'This proposal was closed by a maintainer: ' + reason
              : 'This proposal was closed by a maintainer.';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              state: 'closed',
            });
